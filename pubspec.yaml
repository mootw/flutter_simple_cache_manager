name: flutter_simple_cache_manager
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  path_provider: ^2.0.2
  #Mutex
  synchronized: ^3.0.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter

